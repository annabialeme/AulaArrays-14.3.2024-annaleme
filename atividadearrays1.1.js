let quadrados = numeros.map(numero => {
  //a função da seta que recebe o parametro 'numero' 
  
  return numero * numero;
   //dentro da função, cada numero é multiplicado por si mesmo e é elevado ao quadrado e retornado
 });
 console.log(quadrados);
 //cada elemento do array original é substituido pelo resultado correspondente a função 
