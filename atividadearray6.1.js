//começamos com seis numeros
let numeros = [20, 30, 40, 50, 40, 50];

// ele recebe um parâmetro que é o elemento que desejamos encontrar. Neste caso, queremos encontrar o último índice do número 40.
let ultimoIndice = numeros.lastIndexOf(40);

//é atribuído à variável ultimoIndice, que é então impressa no console.
console.log(ultimoIndice);